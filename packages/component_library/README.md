# Component Library

This package will define:
- category rules
- alias normalization
- package lookup logic
- trusted-library indexing

It should not contain huge raw vendor dumps.

Vendor sources are references.
Reviewed AURA packages are the trusted runtime source.
