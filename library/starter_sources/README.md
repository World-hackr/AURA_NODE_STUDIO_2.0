# Starter Sources

This folder now holds a small selected Fritzing working set for quick Component Lab testing.

It is not the full source library.

The full local Fritzing mirror lives under:

- `vendor_reference/fritzing_paired/`

Use this folder for:

- quick targeted experiments
- early correction workflow tests
- high-priority part review before moving to a broader source sweep

Rules:

- treat these as source assets, not final trusted packages
- use `vendor_reference/fritzing_paired/` as the broad source base
- keep AURA corrections separate from vendor source files
